import org.w3c.dom.ls.LSOutput;

import java.sql.SQLOutput;

public class StringExercise {

    public static void main(String[] args) {

    /* ***************************************
                   STRING BASICS
     ****************************************/

        // TODO: 1. Create a class named StringExercise with a main method.

        // TODO: For each of the following output examples:
        //  create a String variable that contains the desired output and print it out to the console, you can do this with only one String variable and one print statement for each output example.

        // We don't need no education
        // We don't need no thought control
        String firstMessage = "We don't need no education\nWe don't need no thought control";
        System.out.println(firstMessage);

        // Check "this" out!, "s inside of "s!
        String secondMessage = "Check \"this\" out!, \"s inside of \"s!";
        System.out.println(secondMessage);

        // In windows, the main drive is usually C:\
        String thirdMessage = "In windows, the main drive is usually C:\\";
        System.out.println(thirdMessage);

        // I can do backslashes \, double backslashes \\,
        // and the amazing triple backslash \\\!
        String fourthMessage = "I can do backslashes \\, double backslashes \\\\,\nand the amazing triple backslash \\\\\\!";
        System.out.println(fourthMessage);

    }
}